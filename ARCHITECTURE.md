# 🏗️ Agent Skills Guard - 架构设计文档

## 设计哲学：批判与自我批判

### 批判性分析

本项目基于对现有 Claude Code Skills 生态系统的**批判性思考**：

#### 现有方案的问题
1. **安全缺陷**
   - Skills 直接从网络下载并执行，无安全检查
   - 用户无法评估 Skills 的安全风险
   - 缺少代码审计机制

2. **权限滥用风险**
   - Skills 可能包含恶意操作：
     - 文件系统破坏（`rm -rf /`）
     - 敏感数据窃取（读取 `~/.ssh`, `~/.aws`）
     - 网络数据外传
     - 进程执行（命令注入）

3. **管理分散**
   - Skills 管理功能嵌入在大型应用中
   - 难以独立使用和维护
   - 缺少专门的安全工具

### 自我批判：避免过度设计

在设计 MVP 时，我们进行了**自我批判**，避免以下陷阱：

❌ **不做的事情**：
- 复杂的沙箱隔离系统（需要容器技术，过度复杂）
- 云端同步和账号系统（增加维护成本）
- 完整的 CI/CD 集成（超出 MVP 范围）
- 区块链验证机制（技术炫技，无实际价值）

✅ **聚焦的核心**：
- 静态代码分析（正则匹配危险模式）
- 本地数据管理（SQLite）
- 透明的安全评分
- 简洁的用户界面

## 技术架构

### 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                   Presentation Layer                    │
│                    (React Components)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ SkillsPage   │  │ ReposPage    │  │ UI Components│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                   Application Layer                     │
│                   (React Hooks + TanStack Query)        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ useSkills    │  │ useRepos     │  │ API Wrapper  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │ Tauri IPC
┌──────────────────────────▼──────────────────────────────┐
│                     Command Layer                       │
│                   (Tauri Commands)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ add_repo     │  │ scan_repo    │  │ install_skill│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                    Service Layer                        │
│                 (Business Logic)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ GitHubService│  │ SkillManager │  │SecurityScanner│  │
│  │              │  │              │  │              │  │
│  │ - scan_repo  │  │ - install    │  │ - scan_file  │  │
│  │ - download   │  │ - uninstall  │  │ - calc_score │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│                     Data Layer                          │
│                  (Models + Database)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Repository   │  │ Skill        │  │ Security     │  │
│  │ Model        │  │ Model        │  │ Model        │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
│                   SQLite Database                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ repositories │  │ skills       │  │ installations│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 核心模块

#### 1. Security Module (安全模块)

**责任**：检测代码中的危险模式

```rust
SecurityScanner
├── scan_file() -> SecurityReport
│   ├── 逐行扫描代码
│   ├── 应用正则规则
│   └── 计算安全评分
├── calculate_score() -> i32
│   └── 基于问题严重程度扣分
└── calculate_checksum() -> String
    └── SHA256 哈希验证

SecurityRules (规则库)
├── DANGEROUS_FS_PATTERNS
├── NETWORK_PATTERNS
├── DATA_EXFILTRATION_PATTERNS
├── FILE_OPERATION_PATTERNS
└── OBFUSCATION_PATTERNS
```

**评分算法**：

```rust
base_score = 100
for issue in issues:
    match issue.severity:
        Critical => base_score -= 30
        Error    => base_score -= 20
        Warning  => base_score -= 10
        Info     => base_score -= 5

final_score = max(0, base_score)

// 安全等级
score >= 90 => Safe
score >= 70 => Low Risk
score >= 50 => Medium Risk
score >= 30 => High Risk
score < 30  => Critical
```

#### 2. GitHub Service (GitHub 服务)

**责任**：与 GitHub API 交互，扫描仓库

```rust
GitHubService
├── scan_repository() -> Vec<Skill>
│   ├── 获取仓库内容
│   ├── 递归扫描子目录（深度限制：5层）
│   └── 识别 Skill 文件 (.py, .js, .ts, .sh)
├── fetch_directory_contents()
│   └── GET /repos/:owner/:repo/contents/:path
└── download_file() -> Vec<u8>
    └── 下载 raw.githubusercontent.com 内容
```

**限流策略**：
- GitHub API 限制：5000 次/小时（已认证）
- 递归深度限制：5 层
- 文件类型过滤：仅扫描代码文件

#### 3. Skill Manager (Skill 管理器)

**责任**：管理 Skills 的生命周期

```rust
SkillManager
├── download_and_analyze() -> Vec<u8>
│   ├── 下载文件内容
│   ├── 安全扫描
│   └── 更新元数据
├── install_skill()
│   ├── 检查安全评分 (>= 50)
│   ├── 写入 ~/.claude/skills/
│   └── 更新数据库
└── uninstall_skill()
    ├── 删除本地文件
    └── 更新数据库状态
```

**安装策略**：
- 安全评分 < 50：拒绝安装
- 安全评分 >= 50：允许安装，显示警告
- 安全评分 >= 90：推荐安装

#### 4. Database Layer (数据库层)

**Schema 设计**：

```sql
-- 仓库表
CREATE TABLE repositories (
    id TEXT PRIMARY KEY,
    url TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    description TEXT,
    enabled INTEGER DEFAULT 1,
    scan_subdirs INTEGER DEFAULT 1,
    added_at TEXT NOT NULL,
    last_scanned TEXT
);

-- Skills 表
CREATE TABLE skills (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT,
    repository_url TEXT NOT NULL,
    file_path TEXT NOT NULL,
    version TEXT,
    author TEXT,
    installed INTEGER DEFAULT 0,
    installed_at TEXT,
    local_path TEXT,
    checksum TEXT,
    security_score INTEGER,
    security_issues TEXT -- JSON
);

-- 安装记录表
CREATE TABLE installations (
    skill_id TEXT PRIMARY KEY,
    installed_at TEXT NOT NULL,
    version TEXT NOT NULL,
    local_path TEXT NOT NULL,
    checksum TEXT NOT NULL,
    FOREIGN KEY(skill_id) REFERENCES skills(id)
);
```

## 前端架构

### 状态管理

使用 **TanStack Query** 管理服务端状态：

```typescript
// 数据查询
useQuery({
  queryKey: ["skills"],
  queryFn: () => api.getSkills()
})

// 数据变更
useMutation({
  mutationFn: (id) => api.installSkill(id),
  onSuccess: () => {
    queryClient.invalidateQueries(["skills"])
  }
})
```

**优势**：
- 自动缓存和失效
- 乐观更新
- 错误重试
- 加载状态管理

### 组件设计

```
App.tsx (根组件)
├── Header
├── Navigation (Tabs)
└── Pages
    ├── SkillsPage
    │   ├── FilterButtons
    │   └── SkillCard[]
    │       ├── SecurityBadge
    │       ├── InstallButton
    │       └── SecurityIssuesList
    └── RepositoriesPage
        ├── AddRepoForm
        └── RepoCard[]
            ├── ScanButton
            └── DeleteButton
```

## 安全机制

### 威胁模型

**攻击场景**：
1. 恶意 Skill 删除用户文件
2. 窃取 SSH 密钥、API 密钥
3. 建立反向 Shell
4. 挖矿程序
5. 钓鱼攻击

### 防护措施

#### 1. 静态分析

```rust
// 示例：检测危险文件操作
let DANGEROUS_PATTERNS = [
    r"rm\s+-rf\s+/",           // 删除根目录
    r"shutil\.rmtree",         // 递归删除
    r"os\.system\s*\(",        // 系统命令执行
];
```

#### 2. 安全评分透明化

用户可以看到：
- 详细的安全问题列表
- 问题严重程度
- 代码位置（行号）
- 具体代码片段

#### 3. 安装门槛

- 评分 < 50：禁止安装
- 评分 50-69：警告安装
- 评分 70-89：低风险
- 评分 >= 90：安全

## 跨平台支持

### 平台差异处理

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 数据存储 | AppData/Local | ~/Library/Application Support | ~/.local/share |
| Skills 目录 | %USERPROFILE%\\.claude\\skills | ~/.claude/skills | ~/.claude/skills |
| 数据库 | SQLite (bundled) | SQLite (bundled) | SQLite (bundled) |

### 构建配置

```toml
# Cargo.toml
[target.'cfg(target_os = "windows")']
# Windows 特定依赖

[target.'cfg(target_os = "macos")']
# macOS 特定依赖
```

## 性能优化

### 前端优化

1. **代码分割**：使用动态 import
2. **虚拟列表**：大量 Skills 时使用虚拟滚动
3. **缓存策略**：TanStack Query 自动缓存

### 后端优化

1. **异步 I/O**：tokio 运行时
2. **连接池**：rusqlite 连接复用
3. **并发限制**：避免 GitHub API 限流

## 测试策略

### 单元测试

```rust
#[cfg(test)]
mod tests {
    #[test]
    fn test_security_scanner() {
        let scanner = SecurityScanner::new();
        let code = "rm -rf /";
        let report = scanner.scan_file(code, "test.sh").unwrap();
        assert!(report.score < 50);
    }
}
```

### 集成测试

```bash
# Rust 测试
cd src-tauri && cargo test

# 前端测试
pnpm test:unit
```

## 部署架构

```
User Device
├── Tauri App Bundle
│   ├── Frontend (Embedded)
│   ├── Backend (Native Binary)
│   └── SQLite (Bundled)
│
├── Local Storage
│   ├── ~/.agent-skills-guard/
│   │   └── agent-skills.db
│   └── ~/.claude/skills/
│       └── installed-skills/
│
└── Network Access
    └── GitHub API (https://api.github.com)
```

## 未来扩展

### Phase 2
- [ ] 版本管理和更新检测
- [ ] Skills 依赖图分析
- [ ] 更智能的代码分析（AST 解析）

### Phase 3
- [ ] 沙箱隔离运行
- [ ] 社区评分系统
- [ ] 云端配置同步

---

**设计原则总结**：

1. **安全优先**：宁可误报，不可漏报
2. **用户透明**：清晰展示风险，由用户决策
3. **简洁实用**：避免过度设计，聚焦核心功能
4. **批判精神**：持续反思和改进
